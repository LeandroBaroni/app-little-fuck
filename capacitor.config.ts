import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.applittlefuck',
  appName: 'app-little-fuck',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
